// Assessments JavaScript
document.addEventListener("DOMContentLoaded", () => {
  loadAssessments()
})

function loadAssessments() {
  // Assessments are already populated in HTML for demo
  console.log("Assessments loaded")
}
